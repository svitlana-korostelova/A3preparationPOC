# AI

# 1. Problems of integration? (took presentation from Sephora)

- Structural problem . Long .md files == CLAUDE.md constitution should be 150-200 rows, link between md files, crossreference, mindmap(?) in a manner AI have clear hierarchy and applied structured rules . DLC of this files must be  defined, documented and be awared during onboarding. Extract to Skills (//TODO: will add structure between md)
- Couple of modifiers one md file == contradictional rules
- Leaving ‘Its better to prefer structs then classes’ makes AI use this rule not each run, its considered as optional execution == use strong rules ‘must’, prompts ‘do this’ instead of ‘what if we use this?’ and ‘it might be like this’
- Overuse tokens == (//TODO: multiple technics)
- 

We need:

- Every document goes through a managed lifecycle — not 'write once, forget forever'
- Create: mandatory metadata fields, enforced structure, observation tags
Review: deterministic validation on every save - not optional human review
- Link: every cross-reference verified on disk - broken links caught at write time
Monitor: staleness thresholds flag content that hasn't been updated (14-day gate)
Audit: periodic quality sweeps detect contradictions, duplication, drift
The lifecycle is automated. You set it up once and it runs itself.

# 2. Quality checks

- Structural health
- Token efficiency
- Semantic quality
- Agentic Execution
- Platform specific

# 3. Steps of adoptions AI in project? Preconditions?

# Who is verifier of md filer, architecture reviever,

confirmator of file names, link is not broken , 

1. How does AI from Apple currently organised? Does it better then other platforms?